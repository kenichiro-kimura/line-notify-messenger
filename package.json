{
  "name": "line-notify-messenger",
  "version": "1.0.0",
  "description": "A LINE Messaging API bot like LINE Notify service",
  "main": "src/handler.ts",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --outdir=dist --external:node:buffer --external:node:crypto --external:node:stream --format=cjs",
    "deploy": "cdk deploy",
    "watch": "tsc -w",
    "test": "jest",
    "install-deps": "npm install",
    "destroy": "cdk destroy"
  },
  "dependencies": {
    "@line/bot-sdk": "^9.5.3",
    "aws-cdk-lib": "^2.0.0",
    "aws-sdk": "^2.1000.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "@types/node": "^14.0.0",
    "jest": "^29.7.0",
    "typescript": "^4.0.0",
    "esbuild": "^0.14.0"
  },
  "keywords": [
    "LINE",
    "bot",
    "AWS",
    "Lambda",
    "DynamoDB"
  ],
  "author": "Your Name",
  "license": "MIT"
}
